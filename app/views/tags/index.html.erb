<h1>All Tags for <%= current_user.email %></h1>
<ul>
  <% @tags.each do |tag| %>
    <li>
      <h2><%= tag.name %></h2>
      <ul>
        <% tag.bookmarks.each do |bookmark| %>
          <li><%= link_to bookmark.title, bookmark %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
